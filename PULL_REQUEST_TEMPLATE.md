# Backup / limpieza: crear carpetas y utilitario de archivado

Esta es una pull request que aplica únicamente los cambios de creación de carpetas y utilitario de backup del PR #1. Incluye `utils/backup.py`, `startup_backup.py`, `.env.example` y `.gitkeep` para `storage/database/chroma_db`. No incluye cambios de dependencias ni endpoints. Revisa PR #1 y evita duplicar o añadir cosas que ya existan.